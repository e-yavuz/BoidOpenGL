{
    "version": "0.2.0",
    "configurations": [
        {
            
            "type": "lldb",
            "request": "launch",
            "name": "lldb",
            "program": "${fileDirname}/${fileBasenameNoExtension}_debug.exe",
            "cwd": "${fileDirname}",
            "args" : [""],
            "console": "externalTerminal"
        }
    ]
}